---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import '../styles/phycat-abyss.css';

type Props = CollectionEntry<'blog'>['data'];

const { title, description, pubDate, updatedDate, heroImage } = Astro.props;
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={title} description={description} />
		<style>
			/* ===== 莫兰迪配色变量（关于页面） ===== */
			.morandi-theme {
				--morandi-blue: #6b8cae;
				--morandi-green: #8faa8f;
				--morandi-purple: #a68fac;
				--morandi-warm: #b8a090;
				--morandi-cool: #7a8b99;
			}

			.morandi-theme .article-container {
				background:
					radial-gradient(at 0% 0%, rgba(107, 140, 174, 0.08) 0, transparent 50%),
					radial-gradient(at 100% 100%, rgba(143, 170, 143, 0.06) 0, transparent 50%),
					var(--bg-abyss);
			}

			.morandi-theme .back-link:hover {
				color: var(--morandi-blue);
			}

			.morandi-theme .article-title {
				background: linear-gradient(135deg, #e8e8e8 0%, var(--morandi-blue) 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
			}

			.morandi-theme .text-muted {
				color: var(--morandi-cool);
			}

			.article-container {
				background-color: var(--bg-abyss);
				min-height: 100vh;
			}

			/* ===== 阅读进度条 ===== */
			.read-progress {
				position: fixed;
				top: 0;
				left: 0;
				width: 0%;
				height: 2px;
				background: linear-gradient(90deg, var(--primary-cyan), var(--primary-purple));
				z-index: 10000;
				transition: width 0.1s linear;
			}

			.read-progress::after {
				content: '';
				position: absolute;
				right: 0;
				top: 0;
				width: 100px;
				height: 100%;
				background: linear-gradient(90deg, transparent, var(--primary-cyan));
				filter: blur(4px);
			}

			/* ===== 文章头部 ===== */
			.article-header {
				padding: 8rem 0 3rem;
				max-width: 900px;
				margin: 0 auto;
				padding-left: 1.5rem;
				padding-right: 1.5rem;
				position: relative;
			}

			.back-link {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				font-family: 'Cascadia Code', monospace;
				font-size: 0.85rem;
				color: var(--text-muted);
				margin-bottom: 2rem;
				transition: var(--transition-base);
				text-decoration: none;
			}

			.back-link:hover {
				color: var(--primary-cyan);
			}

			.title-section {
				margin-bottom: 2rem;
			}

			.article-title {
				font-size: clamp(2rem, 5vw, 3.5rem);
				font-weight: 800;
				line-height: 1.15;
				margin-bottom: 1.5rem;
				color: var(--text-primary);
				letter-spacing: -0.02em;
			}

			.article-meta {
				display: flex;
				align-items: center;
				gap: 1.5rem;
				font-family: 'Cascadia Code', monospace;
				font-size: 0.85rem;
				color: var(--text-secondary);
				flex-wrap: wrap;
			}

			.meta-item {
				display: flex;
				align-items: center;
				gap: 0.4rem;
			}

			.text-muted {
				opacity: 0.6;
			}

			/* ===== 文章布局 ===== */
			.article-wrapper {
				max-width: 1200px;
				margin: 0 auto;
				display: grid;
				grid-template-columns: 1fr 280px;
				gap: 3rem;
				padding: 0 1.5rem;
			}

			@media (max-width: 1100px) {
				.article-wrapper {
					grid-template-columns: 1fr;
				}
			}

			/* ===== 主内容区 ===== */
			.article-main {
				max-width: 720px;
			}

			.hero-image {
				margin-bottom: 3rem;
				border-radius: var(--radius-md);
				overflow: hidden;
				border: 1px solid var(--border-color);
				box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
			}

			.hero-image img {
				width: 100%;
				height: auto;
				display: block;
			}

			.article-content {
				padding-bottom: 6rem;
			}

			/* Typora Abyss Content Overrides */
			#write {
				background: transparent !important;
				padding: 0 !important;
				box-shadow: none !important;
				color: var(--text-primary) !important;
			}

			:global(.prose) {
				font-size: 1.1rem;
				line-height: 1.85;
			}

			/* ===== 浮动目录 ===== */
			.toc-sidebar {
				position: sticky;
				top: 100px;
				height: fit-content;
				padding: 1.5rem;
				background: rgba(15, 13, 26, 0.6);
				backdrop-filter: blur(10px);
				border: 1px solid rgba(255, 255, 255, 0.05);
				border-radius: 12px;
			}

			@media (max-width: 1100px) {
				.toc-sidebar {
					display: none;
				}
			}

			.toc-title {
				font-family: 'Cascadia Code', monospace;
				font-size: 0.75rem;
				color: var(--text-muted);
				letter-spacing: 0.1em;
				text-transform: uppercase;
				margin-bottom: 1rem;
				padding-bottom: 0.75rem;
				border-bottom: 1px solid rgba(255, 255, 255, 0.05);
			}

			.toc-list {
				list-style: none;
				padding: 0;
				margin: 0;
			}

			.toc-item {
				margin: 0.5rem 0;
			}

			.toc-item-h2 {
				margin-left: 0;
			}

			.toc-item-h3 {
				margin-left: 1rem;
			}

			.toc-link {
				display: block;
				padding: 0.4rem 0.75rem;
				color: var(--text-secondary);
				font-size: 0.85rem;
				text-decoration: none;
				border-radius: 6px;
				transition: all 0.2s;
				border-left: 2px solid transparent;
			}

			.toc-link:hover {
				color: var(--text-primary);
				background: rgba(255, 255, 255, 0.03);
			}

			.toc-link.active {
				color: var(--primary-cyan);
				background: rgba(0, 243, 255, 0.05);
				border-left-color: var(--primary-cyan);
			}

			/* ===== 代码块增强 - 修复底部突出 ===== */
			/* 强制重置代码块基础样式，覆盖 phycat.dark.css 的冲突 */
			:global(#write pre.astro-code) {
				position: relative !important;
				padding: 0 !important;
				border-radius: 10px !important;
				background: #0f111a !important;
				border: 1px solid rgba(255, 255, 255, 0.06) !important;
				margin: 1.5rem 0 !important;
				overflow: auto !important;
			}

			/* 代码块头部栏 */
			:global(#write pre.astro-code > .code-header) {
				position: sticky;
				top: 0;
				left: 0;
				right: 0;
				height: 2.2rem;
				background: rgba(255, 255, 255, 0.02);
				border-bottom: 1px solid rgba(255, 255, 255, 0.04);
				display: flex;
				align-items: center;
				justify-content: space-between;
				padding: 0 0.75rem 0 5rem;
				font-family: 'Cascadia Code', monospace;
				font-size: 0.7rem;
				color: var(--text-muted);
				text-transform: uppercase;
				z-index: 10;
			}

			/* 代码内容区域 - 覆盖 phycat.dark.css 的行内代码样式 */
			:global(#write pre.astro-code > code),
			:global(#write pre.astro-code code:not(.md-fencescode)) {
				display: block !important;
				padding: 1rem 2.5rem 1rem 1.25rem !important;
				font-size: 0.875rem !important;
				line-height: 1.6 !important;
				background: transparent !important;
				/* 覆盖 phycat.dark.css 的行内代码样式 */
				border: none !important;
				margin: 0 !important;
				border-radius: 0 !important;
				transform: none !important;
				transition: none !important;
				width: auto !important;
				min-width: fit-content !important;
			}

			/* 强制覆盖 phycat.dark.css 的 hover 效果 - 增强选择器 specificity */
			:global(#write pre.astro-code > code:hover),
			:global(#write pre.astro-code:hover > code),
			:global(#write pre.astro-code > code:not(.md-fencescode):hover) {
				background: transparent !important;
				background-color: transparent !important;
				border: none !important;
				transform: none !important;
				box-shadow: none !important;
				color: inherit !important;
			}

			/* 更强力的覆盖 - 直接针对 pre.astro-code 内部的所有 code 元素 */
			:global(pre.astro-code code),
			:global(pre.astro-code code:hover) {
				background: transparent !important;
				background-color: transparent !important;
				border: none !important;
				transform: none !important;
				box-shadow: none !important;
				color: inherit !important;
				padding: 0 !important;
				margin: 0 !important;
				border-radius: 0 !important;
			}

			/* 复制按钮 - 极简设计，无背景 */
			:global(.code-header .code-copy-btn),
			.code-copy-btn {
				appearance: none !important;
				-webkit-appearance: none !important;
				-moz-appearance: none !important;
				position: absolute !important;
				top: 50% !important;
				right: 0.75rem !important;
				transform: translateY(-50%) !important;
				width: 22px !important;
				height: 22px !important;
				display: flex !important;
				align-items: center !important;
				justify-content: center !important;
				background: transparent !important;
				background-color: transparent !important;
				border: none !important;
				border-radius: 0 !important;
				color: rgba(255, 255, 255, 0.5) !important;
				cursor: pointer !important;
				opacity: 0.6 !important;
				transition: opacity 0.2s, transform 0.2s !important;
				z-index: 20 !important;
				box-shadow: none !important;
				padding: 0 !important;
				margin: 0 !important;
				font-size: 14px !important;
				line-height: 1 !important;
			}

			:global(.code-header .code-copy-btn:hover),
			.code-copy-btn:hover {
				background: transparent !important;
				background-color: transparent !important;
				border: none !important;
				border-radius: 0 !important;
				color: rgba(255, 255, 255, 0.9) !important;
				opacity: 1 !important;
				transform: translateY(-50%) scale(1.1) !important;
				box-shadow: none !important;
			}

			:global(.code-header .code-copy-btn.copied),
			.code-copy-btn.copied {
				background: transparent !important;
				background-color: transparent !important;
				border: none !important;
				border-radius: 0 !important;
				color: #4ade80 !important;
				opacity: 1 !important;
				box-shadow: none !important;
			}

			/* 代码窗口按钮 - 固定在头部 */
			:global(pre.astro-code > .code-header::before) {
				content: '';
				position: absolute;
				left: 1rem;
				top: 50%;
				transform: translateY(-50%);
				width: 8px;
				height: 8px;
				border-radius: 50%;
				background: #ff5f56;
				box-shadow:
					14px 0 0 #ffbd2e,
					28px 0 0 #27c93f;
			}

			/* 阅读时间估计 */
			.reading-time {
				display: inline-flex;
				align-items: center;
				gap: 0.4rem;
				color: var(--text-muted);
			}
		</style>
	</head>

	<body>
		<div class="read-progress" id="readProgress"></div>

		<div class={`article-container ${title === 'About Me' ? 'morandi-theme' : ''}`}>
			<Header />

			<main>
				<article>
					<header class="article-header">
						<a href="/blog" class="back-link">
							<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<polyline points="15 18 9 12 15 6"></polyline>
							</svg>
							返回归档
						</a>
						<div class="title-section">
							<h1 class="article-title" id="articleTitle">{title}</h1>
							<div class="article-meta">
								<div class="meta-item">
									<span class="text-muted">发布于</span>
									<FormattedDate date={pubDate} />
								</div>
								{updatedDate && (
									<div class="meta-item">
										<span class="text-muted">更新于</span>
										<FormattedDate date={updatedDate} />
									</div>
								)}
								<div class="meta-item reading-time" id="readingTime">
									<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
										<circle cx="12" cy="12" r="10"></circle>
										<polyline points="12 6 12 12 16 14"></polyline>
									</svg>
									<span id="readingTimeText">计算中...</span>
								</div>
							</div>
						</div>
					</header>

					<div class="article-wrapper">
						<div class="article-main">
							{heroImage && (
								<div class="hero-image">
									<Image width={1000} height={500} src={heroImage} alt={title} />
								</div>
							)}

							<div class="article-content">
								<div id="write" class="prose">
									<slot />
								</div>
							</div>
						</div>

						<aside class="toc-sidebar" id="tocSidebar">
							<div class="toc-title">目录导航</div>
							<nav class="toc-nav">
								<ul class="toc-list" id="tocList">
									<!-- 动态生成 -->
								</ul>
							</nav>
						</aside>
					</div>
				</article>
			</main>

			<Footer />
		</div>

		<script>
			// ===== 阅读进度条 =====
			function updateReadingProgress() {
				const scrollTop = window.scrollY;
				const docHeight = document.documentElement.scrollHeight - window.innerHeight;
				const progress = (scrollTop / docHeight) * 100;
				document.getElementById('readProgress')!.style.width = `${progress}%`;
			}

			window.addEventListener('scroll', updateReadingProgress, { passive: true });

			// ===== 阅读时间计算 =====
			function calculateReadingTime() {
				const articleText = document.getElementById('write')?.textContent || '';
				const wordCount = articleText.trim().length;
				const readingTime = Math.ceil(wordCount / 300);
				const text = readingTime < 1 ? '少于 1 分钟' : `${readingTime} 分钟阅读`;
				document.getElementById('readingTimeText')!.textContent = text;
			}

			// ===== 生成目录 =====
			function generateTOC() {
				const article = document.getElementById('write');
				const tocList = document.getElementById('tocList');
				if (!article || !tocList) return;

				const headings = article.querySelectorAll('h2, h3');
				if (headings.length === 0) {
					document.getElementById('tocSidebar')!.style.display = 'none';
					return;
				}

				headings.forEach((heading, index) => {
					const id = `heading-${index}`;
					heading.id = id;

					const li = document.createElement('li');
					li.className = `toc-item toc-item-${heading.tagName.toLowerCase()}`;

					const link = document.createElement('a');
					link.className = 'toc-link';
					link.href = `#${id}`;
					link.textContent = heading.textContent;
					link.dataset.target = id;

					link.addEventListener('click', (e) => {
						e.preventDefault();
						heading.scrollIntoView({ behavior: 'smooth', block: 'start' });
					});

					li.appendChild(link);
					tocList.appendChild(li);
				});
			}

			// ===== 高亮当前章节 =====
			function highlightCurrentHeading() {
				const headings = document.querySelectorAll('#write h2, #write h3');
				const tocLinks = document.querySelectorAll('.toc-link');

				let currentId = '';
				headings.forEach(heading => {
					const rect = heading.getBoundingClientRect();
					if (rect.top <= 120) {
						currentId = heading.id;
					}
				});

				tocLinks.forEach(link => {
					const el = link as HTMLElement;
					el.classList.toggle('active', el.dataset.target === currentId);
				});
			}

			window.addEventListener('scroll', highlightCurrentHeading, { passive: true });

			// ===== 代码块复制按钮 =====
			function addCopyButtons() {
				const codeBlocks = document.querySelectorAll('pre.astro-code');

				codeBlocks.forEach(block => {
					// 获取语言标签
					const lang = block.getAttribute('data-language') || 'code';

					// 获取代码元素
					const codeEl = block.querySelector('code');
					if (!codeEl) return;

					// 创建头部栏
					const header = document.createElement('div');
					header.className = 'code-header';

					// 创建语言标签
					const langSpan = document.createElement('span');
					langSpan.textContent = lang;

					// 创建复制按钮
					const button = document.createElement('button');
					button.className = 'code-copy-btn';
					button.innerHTML = `
						<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
							<path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
						</svg>
					`;
					button.setAttribute('aria-label', '复制代码');

					button.addEventListener('click', async () => {
						const code = codeEl.textContent || '';
						try {
							await navigator.clipboard.writeText(code);
							button.classList.add('copied');
							button.innerHTML = `
								<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
									<polyline points="20 6 9 17 4 12"></polyline>
								</svg>
							`;
							setTimeout(() => {
								button.classList.remove('copied');
								button.innerHTML = `
									<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
										<rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
										<path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
									</svg>
								`;
							}, 2000);
						} catch (err) {
							console.error('复制失败:', err);
						}
					});

					// 组装头部栏
					header.appendChild(langSpan);
					header.appendChild(button);

					// 将头部栏插入到代码块最前面
					block.insertBefore(header, block.firstChild);
				});
			}

			// ===== 自动转换 GFM 警告框标记 =====
			function transformAlerts() {
				const blocks = document.querySelectorAll('#write blockquote');
				blocks.forEach(block => {
					const firstP = block.querySelector('p');
					if (!firstP) return;

					const content = firstP.textContent || '';
					const match = content.match(/^\[!(NOTE|TIP|WARNING|IMPORTANT|CAUTION)\]/i);

					if (match) {
						const type = match[1].toLowerCase();
						block.classList.add('markdown-alert', `markdown-alert-${type}`);

						firstP.innerHTML = firstP.innerHTML.replace(/^\[!(NOTE|TIP|WARNING|IMPORTANT|CAUTION)\]/i, '').trim();

						if (!firstP.textContent?.trim() && !firstP.querySelector('img')) {
							firstP.remove();
						}
					}
				});
			}

			// ===== 初始化 =====
			document.addEventListener('DOMContentLoaded', () => {
				calculateReadingTime();
				generateTOC();
				addCopyButtons();
				transformAlerts();
				highlightCurrentHeading();
			});
		</script>
	</body>
</html>